# Ingest-draft A0 packages built from real Sketchfab-exported GLBs.
#
# Status is `ingest-draft`: GLB + preview are real; USDZ remains a placeholder
# until a pinned GLB→USDZ toolchain is available. License SPDX is
# LicenseRef-PendingVerification until source pages are confirmed.
