{
  "name": "@visioncart/asset-tooling",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run asset-cli --help",
    "lint": "uv run ruff check .",
    "typecheck": "uv run mypy src",
    "test": "uv run pytest"
  }
}
